{
  "name": "contracts",
  "version": "1.0.0",
  "type": "module",
  "description": "Smart contracts for Data Vault Platform",
  "scripts": {
    "compile": "hardhat compile",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "ethers": "^6.15.0",
    "forge-std": "^1.1.2",
    "hardhat": "^2.26.2"
  }
}
